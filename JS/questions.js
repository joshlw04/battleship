/*
displayGameOver() {
  if (this.board.isGameOver() === true) {
    document.querySelector('#shots-fired').innerHTML = 'You found all the ships!';
  }
}

where should this go, in order for it to work? I can't call it in the console to test.




*/
